# Quick Start

1. Install the module
1. Run dev/build + flush
1. Navigate to the Listing CMS section and create a listing template
1. Create a new listing page, setting appropriate values
1. Add the `{$Listing}` keyword to the page's Content block
